{% extends 'base.html.twig' %}

{% block title %}🔥 Най-добрите оферти - Технологии и Мода{% endblock %}

{% block body %}
    <div class="container-fluid bg-dark text-white py-5 mb-5" style="background: linear-gradient(135deg, #1a202c 0%, #2d3748 100%);">
        <div class="container text-center">
            <h1 class="display-3 fw-black mb-3">ЕКСКЛУЗИВНИ <span style="color: #f093fb;">ОФЕРТИ</span></h1>
            <p class="lead opacity-75">Ръчно подбрани промоции от най-големите магазини</p>
        </div>
    </div>

    <div class="container" id="deals">
        <div class="row g-4">
            {% for review in reviews %}
                <div class="col-6 col-md-4 col-lg-3">
                    <div class="card h-100 border-0 shadow-sm rounded-4 overflow-hidden product-hover-effect">

                        <div class="position-absolute top-0 start-0 m-2" style="z-index: 10;">
                            {% if 'fashiondays' in review.originalProductUrl %}
                                <span class="badge bg-dark px-2 py-1 shadow-sm">Fashion Days</span>
                            {% elseif 'emag' in review.originalProductUrl %}
                                <span class="badge bg-primary px-2 py-1 shadow-sm">eMAG</span>
                            {% elseif 'alleop' in review.originalProductUrl %}
                                <span class="badge bg-success px-2 py-1 shadow-sm">Alleop</span>
                            {% endif %}
                        </div>

                        <div class="bg-white d-flex align-items-center justify-content-center p-3" style="height: 200px;">
                            <a href="{{ path('app_buy_redirect', {id: review.id}) }}" target="_blank">
                                <img src="{{ review.imageUrl }}" class="img-fluid" style="max-height: 180px; object-fit: contain;" alt="{{ review.title }}">
                            </a>
                        </div>

                        <div class="card-body d-flex flex-column">
                            <h6 class="card-title fw-bold text-dark mb-2" style="height: 40px; overflow: hidden;">
                                {{ review.title }}
                            </h6>

                            <div class="mt-auto">
                                <div class="d-flex justify-content-between align-items-end mb-3">
                                    <span class="fs-4 fw-black text-danger">{{ review.price }} лв.</span>
                                    <small class="text-muted text-decoration-line-through">verified</small>
                                </div>

                                <a href="{{ path('app_review_show', {slug: review.slug}) }}" class="btn btn-outline-dark w-100 rounded-3 fw-bold mb-2">
                                    ДЕТАЙЛИ
                                </a>
                                <a href="{{ path('app_buy_redirect', {id: review.id}) }}" target="_blank" class="btn btn-primary w-100 rounded-3 fw-bold shadow-sm" style="background: #667eea; border: none;">
                                    КЪМ МАГАЗИНА
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            {% else %}
                <div class="col-12 text-center py-5">
                    <div class="display-1 text-muted mb-4">🏜️</div>
                    <h3 class="text-secondary">Няма намерени продукти за този източник.</h3>
                    <a href="{{ path('app_home') }}" class="btn btn-primary mt-3">Виж всички</a>
                </div>
            {% endfor %}
        </div>

        <div class="d-flex justify-content-center my-5">
            {{ knp_pagination_render(reviews) }}
        </div>
    </div>

    <style>
        .fw-black { font-weight: 900; }
        .product-hover-effect { transition: transform 0.3s ease, box-shadow 0.3s ease; }
        .product-hover-effect:hover { transform: translateY(-8px); box-shadow: 0 15px 30px rgba(0,0,0,0.1) !important; }

        /* Стилизиране на пагинацията на KNP */
        .pagination { gap: 5px; }
        .page-item .page-link { border-radius: 8px; color: #667eea; font-weight: bold; border: none; }
        .page-item.active .page-link { background-color: #667eea; color: white; }
    </style>
{% endblock %}
